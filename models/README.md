# models/

Per-model records land here from the nightly export (one JSON file per model: quantization sizes with sources, architecture fields used for the context-cache formula, acceleration flags, license). Empty until the export job ships at launch. The schema is described in `FORMULAS.md` §3 and in the tool's architecture notes.
